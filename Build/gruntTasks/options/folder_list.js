module.exports = {
    options: {
        // Default options, you dont need these they are just to highlight the options available.
        files: true,
        folders: true
    },
    files: {
        // Create a JSON file (.json)
        'testFileList.json': ['../Javascript/testData/contracts/**']
    }
}